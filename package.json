{
  "name": "headers-utils",
  "version": "1.2.0",
  "description": "Utilities for working with a Headers instance",
  "main": "lib/index.js",
  "types": "lib",
  "repository": "https://github.com/mswjs/headers-utils",
  "author": "Artem Zakharchenko",
  "license": "MIT",
  "scripts": {
    "clean": "rimraf lib",
    "build": "yarn clean && tsc --build",
    "test": "jest",
    "prepublishOnly": "yarn test && yarn build"
  },
  "files": [
    "lib",
    "README.md"
  ],
  "devDependencies": {
    "@types/jest": "^25.2.1",
    "jest": "^26.0.1",
    "rimraf": "^3.0.2",
    "ts-jest": "^25.5.0",
    "typescript": "^3.8.3"
  }
}
